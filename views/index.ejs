<%- include('partials/header') %>

    <body>

        <%- include('partials/navbar')%>


            <div class="container">
                <hr>
                <h1>Perguntar</h1>
                <hr>
                <a href="/Perguntar" class="btn btn-outline-success">Perguntar</a>
          
                <% perguntas.forEach(pergunta=> { %><!-- estrutura do forEachc  listando pergunta no frontend-->
                    
                    <div clss="card">
                        <div class="card-body">
                            <h3>
                                <%= pergunta.titulo %>
                            </h3>
                        </div>
                        <div class="card.footer">
                            <button class="btn btn-outline-success">Responder</button>
                        </div>

                        <% }) %>
                            

                    </div>

    </body>
    <%- include('partials/footer') %>